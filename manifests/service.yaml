apiVersion: v1
kind: Service
metadata:
  name: wf1
  namespace: namespace-workflow-1728691289028
  labels:
    kubernetes.azure.com/generator: devhub
spec:
  type: LoadBalancer
  selector:
    app: wf1
  ports:
    - protocol: TCP
      port: 8000
      targetPort: 8000